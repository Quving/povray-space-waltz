#include "models/human.inc"

object {
    // #local LEFT_LOWER_ARM_ROT = x*90;
    #local DEBUG_ALL_JOINTS = true;
    // #local LEFT_ARM_ROT = z * 90;
    // #local RIGHT_ARM_ROT = z * 90;
    // #local LEFT_LOWER_ARM_ROT = y * 45;

    Human()
}
